# Legacy Sources

Record previous BPLS source-code evidence here.

If the legacy source is supplied as an archive, store the authoritative archive unchanged and record its checksum. If the legacy source is accessed from Git, record repository URL, branch, baseline commit SHA, date captured, and provenance.

Do not treat legacy architecture as a requirement merely because it exists. Treat it as evidence of implementation behavior.
